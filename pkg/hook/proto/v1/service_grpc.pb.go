// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: pluggableharness/hook/v1/service.proto

// Package pluggableharness.hook.v1 defines the hook-dispatch RPC surface
// described in agent-loop/hook-dispatch.md and architecture.md §Hook
// dispatch semantics: the wire contract the kernel uses to invoke any
// plugin (of any of the six categories) that declares a `hook{}` block in
// agent.hcl. This is deliberately one shared service rather than a
// per-category RPC — hashicorp/go-plugin (.claude/rules/plugin-runtime.md)
// muxes multiple gRPC services over one broker connection, so the kernel
// dials HookSubscriberService on the same subprocess that already serves
// that plugin's own category service. A plugin with no `hook{}` blocks in
// agent.hcl simply never has it called.
//
// context-assemble is deliberately absent from this surface's hook-point
// vocabulary (common.v1.HookPoint) — it stays on ContextService.Contribute
// (context/protocol.md#contribute-the-context-assemble-rpc; architecture.md
// §Hook dispatch semantics), which already carries the full accumulated
// ContextSection chain and doesn't need a second, competing dispatch path.
// This surface serves the other eight hook points only.

package hookv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	HookSubscriberService_DispatchHook_FullMethodName = "/pluggableharness.hook.v1.HookSubscriberService/DispatchHook"
)

// HookSubscriberServiceClient is the client API for HookSubscriberService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// HookSubscriberService is the hook-dispatch protocol described in
// agent-loop/hook-dispatch.md: the kernel invokes a subscribing plugin
// once per hook-point firing it's declared for, in agent.hcl declaration
// order alongside every other subscriber at that point
// (agent-loop/hook-dispatch.md#dispatch-order-and-payload-flow). Every
// plugin category MAY implement this service; whether the kernel ever
// dials it for a given plugin process is entirely a function of that
// plugin's own agent.hcl `hook{}` declarations, not its category.
type HookSubscriberServiceClient interface {
	// DispatchHook delivers one hook-point firing to one subscriber. Unary,
	// not streaming: one invocation is one request/one response
	// (agent-loop/hook-dispatch.md#dispatch-order-and-payload-flow). Per-
	// subscriber timeout is a ctx deadline the kernel sets
	// (.claude/rules/grpc.md "Context and deadlines";
	// agent-loop/hook-dispatch.md#timeout-behavior), not a wire field on
	// this request. Cancellation (the kernel closing the call because the
	// turn is being aborted) is normal control flow, per
	// .claude/rules/grpc.md — never logged as a failure.
	DispatchHook(ctx context.Context, in *DispatchHookRequest, opts ...grpc.CallOption) (*DispatchHookResponse, error)
}

type hookSubscriberServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewHookSubscriberServiceClient(cc grpc.ClientConnInterface) HookSubscriberServiceClient {
	return &hookSubscriberServiceClient{cc}
}

func (c *hookSubscriberServiceClient) DispatchHook(ctx context.Context, in *DispatchHookRequest, opts ...grpc.CallOption) (*DispatchHookResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(DispatchHookResponse)
	err := c.cc.Invoke(ctx, HookSubscriberService_DispatchHook_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// HookSubscriberServiceServer is the server API for HookSubscriberService service.
// All implementations must embed UnimplementedHookSubscriberServiceServer
// for forward compatibility.
//
// HookSubscriberService is the hook-dispatch protocol described in
// agent-loop/hook-dispatch.md: the kernel invokes a subscribing plugin
// once per hook-point firing it's declared for, in agent.hcl declaration
// order alongside every other subscriber at that point
// (agent-loop/hook-dispatch.md#dispatch-order-and-payload-flow). Every
// plugin category MAY implement this service; whether the kernel ever
// dials it for a given plugin process is entirely a function of that
// plugin's own agent.hcl `hook{}` declarations, not its category.
type HookSubscriberServiceServer interface {
	// DispatchHook delivers one hook-point firing to one subscriber. Unary,
	// not streaming: one invocation is one request/one response
	// (agent-loop/hook-dispatch.md#dispatch-order-and-payload-flow). Per-
	// subscriber timeout is a ctx deadline the kernel sets
	// (.claude/rules/grpc.md "Context and deadlines";
	// agent-loop/hook-dispatch.md#timeout-behavior), not a wire field on
	// this request. Cancellation (the kernel closing the call because the
	// turn is being aborted) is normal control flow, per
	// .claude/rules/grpc.md — never logged as a failure.
	DispatchHook(context.Context, *DispatchHookRequest) (*DispatchHookResponse, error)
	mustEmbedUnimplementedHookSubscriberServiceServer()
}

// UnimplementedHookSubscriberServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedHookSubscriberServiceServer struct{}

func (UnimplementedHookSubscriberServiceServer) DispatchHook(context.Context, *DispatchHookRequest) (*DispatchHookResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method DispatchHook not implemented")
}
func (UnimplementedHookSubscriberServiceServer) mustEmbedUnimplementedHookSubscriberServiceServer() {}
func (UnimplementedHookSubscriberServiceServer) testEmbeddedByValue()                               {}

// UnsafeHookSubscriberServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to HookSubscriberServiceServer will
// result in compilation errors.
type UnsafeHookSubscriberServiceServer interface {
	mustEmbedUnimplementedHookSubscriberServiceServer()
}

func RegisterHookSubscriberServiceServer(s grpc.ServiceRegistrar, srv HookSubscriberServiceServer) {
	// If the following call panics, it indicates UnimplementedHookSubscriberServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&HookSubscriberService_ServiceDesc, srv)
}

func _HookSubscriberService_DispatchHook_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(DispatchHookRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HookSubscriberServiceServer).DispatchHook(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HookSubscriberService_DispatchHook_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HookSubscriberServiceServer).DispatchHook(ctx, req.(*DispatchHookRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// HookSubscriberService_ServiceDesc is the grpc.ServiceDesc for HookSubscriberService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var HookSubscriberService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "pluggableharness.hook.v1.HookSubscriberService",
	HandlerType: (*HookSubscriberServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "DispatchHook",
			Handler:    _HookSubscriberService_DispatchHook_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "pluggableharness/hook/v1/service.proto",
}
